import { Stack } from '@mui/material';
import { PropsWithChildren } from 'react';

export const DesktopMenuItemContainer = ({ children }: PropsWithChildren) => {
  return <Stack>{children}</Stack>;
};
